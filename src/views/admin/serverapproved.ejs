<%- include('static/head.ejs', { bot,user,path})  %>
<div id="content" class="main-content">
    <div class="layout-px-spacing">

        <div class="page-header">
            <nav class="breadcrumb-one" aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="javascript:void(0);">Bot List</a></li>
                    <li class="breadcrumb-item active"><a href="javascript:void(0);">Bots aprobados</a></li>
                </ol>
            </nav>
        </div>
        
        <div class="row layout-top-spacing" id="cancel-row">
        
            <div class="col-xl-12 col-lg-12 col-sm-12  layout-spacing">
                <div class="widget-content widget-content-area br-6">
                    <table id="default-ordering" class="table table-hover" style="width:100%">
                        <thead>
                            <tr>
                                <th>Nombre del bot</th>
                                <th>Owner</th>
                                <th class="text-center dt-no-sorting">Acción</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% serverdata.filter(a => a.status === "Approved").map(b => {%>
                            <tr>
                                <td><%= b.name %></td>
                                <td><%= b.ownerID %></td>
                                <td class="text-center">
                                    <button onclick="newtab('/admin/server/delete/<%= b.id %>')" class="btn btn-danger btn-sm">Borrar</button>
                                </td>
                            </tr>
                            <% }) %>
                        </tbody>
                        <tfoot>
                            <tr>
                                <th>Nombre del bot</th>
                                <th>Owner</th>
                                <th>Invite</th>
                                <th class="invisible"></th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
        <script>
            var urlParams = new URLSearchParams(window.location.search);
            if(urlParams.has('success') && urlParams.get('success')){
            Swal.fire({
             icon: 'success',
             title: 'Exitoso...',
             closeOnConfirm: false,
             text: urlParams.get('message'),
             footer: 'Copyright 2021 &copy BotCity - Todos los derechos reservados..'
            }).then((result) => {
          if (result.isConfirmed) {
            window.location.href = `/admin/approvedservers`;
          }
        })
            }
            if(urlParams.has('error') && urlParams.get('error')){
            Swal.fire({
             icon: 'error',
             title: 'Oops... Ha ocurrido un error',
             text: urlParams.get('message'),
             footer: 'Copyright 2021 &copy BotCity - Todos los derechos reservados.'
            }).then((result) => {
          if (result.isConfirmed) {
            window.location.href = '/admin/approvedservers';
          }    
        })
            }
           </script>
        </div></div>
        <%- include('static/foot.ejs', { bot,user,path}) %>